apiVersion: v1
kind: Service
metadata:
  name: weather-gateway-service
  labels:
    app: weather-gateway
spec:
  type: LoadBalancer
  selector:
    app: weather-gateway
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5000
