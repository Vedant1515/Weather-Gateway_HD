apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: weather-data-pvc
  labels:
    app: weather-gateway
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 1Gi
